server:
    port: 8091

spring:
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/Tato-Microservice-Demo/ms-course-geraldo
                    searchPath: config-data
                    username: ${GIT_USERNAME}
                    password: ${GIT_PASSWORD}
                    default-label: "master"
            bootstrap: true

    profiles:
        active: git
    security:
        user:
            name: root
            password: s3cr3t
logging:
    level:
        org.springframework.cloud: DEBUG